# docx-editor-react — entry-point deep review

The plan for the file-by-file deep review of the vendored `docx-editor-react`. **Supersedes the folder-walk audit plan** in `editor-audit.md` (kept for its shipped Phase 1/2 history + the bug checklist + keep-notes, which we mine as we go). New plan because the old one mis-scoped: it walked the package by folder and judged "used?" per file in isolation, which silently kept orphaned chains alive — e.g. `table-style-presets.ts` *looks* used (`useTableDialogs` imports `getBuiltinTableStyle`), but the `applyTableStyle` `TableAction` it serves has **no emitter** since Phase 1 deleted `TableStyleGallery`, so the whole chain is dead. A leaf read can't see that. Start from the entry instead.

## The method — reachability from Patrick's real contract

Patrick is the only consumer. The editor's live surface is exactly what Patrick touches; **everything not reachable from that contract is dead and gets cut** (with its whole chain, in one coherent PR).

**Patrick's contract with `docx-editor-react`** (discovered from `apps/` import sites — `docx-viewer.tsx`, `zoom-pill.tsx`, `active-editor.tsx`, `agent-chat.tsx`, `apps/api/.../chat.ts`):
- **`<DocxEditor>` props passed:** `ref` · `documentBuffer` · `author` · `colorMode` · `onChange` · `onOpenLink` · `renderTitleBarRight` · `loadingIndicator` · `readOnly`. **Nothing else** (all the other declared chrome props were already trimmed/flagged in Phase 2).
- **`DocxEditorRef` methods called:** `save()` · `getAgent()` · `getCurrentPage()` · `getTotalPages()` · `setZoom()` · `openFind()`. **Six, total.**
- **Agent path:** `useDocxAgentTools({ editorRef }).executeToolCall` (agents/react) drives the editor's `DocumentAgent` *through that same ref*; the api side uses `getAiSdkTools` (tool schemas) + `DocxReviewer` (headless). So the agent's reach into the editor = whatever the `DocumentAgent` methods call.
- **CSS:** `@eigenpal/docx-editor-core/styles/editor.css`.

**Per reachable file, the verdict is five questions:** used? · fit for purpose? · worth keeping? · elegant/maintainable or rewrite? · right home? Fold in the matching bug/keep-note from `editor-audit.md` as the per-file checklist (the deep read re-derives them anyway).

## How we run it — SUPERVISED, one coherent unit per PR

A whole-phase unsupervised run was tried and **failed** (a `phase4` integration branch with ~16 self-merged sub-PRs + two stray commits straight to `main`; too much to diagnose, broke the git rules; reverted — `main` reset to #95, branches deleted). **Abandoned.** Now:

- **One file, or one coherent dead/refactor chain, per branch off `main`.** Small enough that the whole diff reviews in one sitting.
- Per unit: deep-read → 5-question verdict → fix/cut → `pnpm check` + `bun test` green → `/code-review` → PR **to `main`** → **the user reviews and merges.**
- **No integration branch. No self-merge. No commits to `main`. NO Claude attribution** (see [[git-release-workflow]]). Claude proposes the merge at a green checkpoint; the user decides.
- **Stop and surface after each merge** — short "what changed / what's next" menu; user picks the next unit. Never chain units autonomously.
- **Grep/codegraph-verify reachability before cutting** (no agent fleets — the cost lesson holds). Behavioural changes go to the user's **smoke-test list**.

## Traversal order — follow the spine inward from the entry

Reachability is decided top-down, so review in dependency order, not folder order:

1. **`components/DocxEditor.tsx`** (the forwardRef brain, ~1640 lines) — **the next read.** It defines `DocxEditorRef` and wires every prop/ref-method to its hook/overlay. Reading it first turns every downstream "used?" from a guess into a fact and exposes the orphaned wires (the table-style chain, any handler with no emitter). Map: each of the 6 ref methods + 9 props → what it touches.
2. **The toolbar / action emitters** it mounts — reconcile every `TableAction` / `FormatAction` *member* against an actual emitter; unmatched members + their handler branches are dead.
3. **The hooks** each surviving action lands in (`components/DocxEditor/hooks/` 32 + top-level `hooks/` 8).
4. **Overlays** (4) + **leaf `internals/`** (11) last — by then reachability is known, so dead chains (e.g. table-style) get cut with their roots, not piecemeal.

## Phase 2 — relocate + per-file deep-read into `components/editor/`

The file-level dead-code well is dry (units 1–8 cleared the reachable dead surface; an orphan scan of the engine found nothing). New method (user, 2026-06-30): **walk the engine's dependency tree from `DocxEditor.tsx` outward, one file per unit — relocate each into `components/editor/` with a kebab-case filename (exported symbol names UNCHANGED), deep-read + clean it, repoint imports.** The old `components/DocxEditor.tsx` + `components/DocxEditor/` folder dissolve into `components/editor/` as files are touched. `components/toolbar/` is Patrick's own UI — NOT touched. Fast cadence: `pnpm check` + `bun test`, no heavy `/code-review` for mechanical relocates (smoke only where behaviour could shift). Relative-import depth shifts by one `../` per file moved a level deeper.

- **DONE (awaiting smoke-test + merge) · Unit 10 — HF EDITING → READ-ONLY (branch `refactor/editor-hf-readonly`).** The user reversed the earlier "properly implement HF" decision (both editing paths were broken; not worth the ~1,140-line tangle) and chose to **strip HF editing back to render-only**, deferring editable H/F as a future feature. Removed ~896 lines: **DELETED** `InlineHeaderFooterEditor.tsx` (467), `HiddenHeaderFooterPMs.tsx` (388), `hooks/useHeaderFooterEditing.ts` (284). **Stripped HF editing out of**: `editor/docx-editor.tsx` (replaced `useHeaderFooterEditing` with an inlined `resolveHeaderFooter` from core for read-only content; removed `hfEditPosition`/`hfEditIsFirstPage`/`hfEditorRef`/`hfVersion` state + the `getHfPmViews` HF-view scan in `onAccept/RejectChangeById` → body-only), `DocxEditorPagedArea.tsx` (dropped all 13 HF props + the inline-editor portal), `PagedEditor.tsx`, `useActiveEditor.ts`, `usePagedEditorRefApi.ts` (dropped `getHfPmView(s)`/`findRidForHeaderFooter`), `useResetEditorState.ts` (dropped the 2 HF setters). **Dead optional HF params removed** from `usePagesPointer.ts` (`hfEditMode`/`getHfView`/`onBodyClick`/`onHeaderFooterDoubleClick` + `wrapEditorViewAsSurface` + the HF coord-space fallback in `getPositionFromMouse` — `activeSurface()` collapses to body PM) and `useLayoutPipeline.ts` (`getHfPmDoc` plumbing → painter always paints `hf.content`). Core untouched: `computeLayout`'s `getHfPmDoc` seam stays, fed `() => null`; `detectTableInsertHover` fed `hfEditMode: null`. CLAUDE.md's "H/F render-only" line is accurate again. `pnpm check` green, **1726 pass**. **Smoke-test (read-only):** open a docx WITH a pre-existing header/footer → renders; clicking/double-clicking the H/F area does nothing (no inline editor); typing in the body unaffected; tables, comments, context-menu, find still work. Then commit → PR → merge.

- **SHIPPED · Unit 13 — relocate orchestrator hooks into `editor/hooks/` (branch `refactor/editor-relocate-hooks`).** Hook-scan first (user's "easy wins" hunch): `DocxEditor/hooks/` is **clean — no shims, no dead hooks**; all 29 are single-caller-by-design (the orchestrator decomposition), so nothing to cut. The win was the relocation. Partitioned the hooks by reachability: **21 "orchestrator" hooks** (consumed only from already-moved `editor/` files) moved to `editor/hooks/` (camelCase kept per user, exports unchanged); the **8 "PagedEditor-subtree" hooks** (`useLayoutPipeline`/`usePagesPointer`/`usePagedScrollApi`/`usePagedEditorRefApi`/`useSelectionOverlay`/`useLayoutTriggers`/`useImageInteractions`/`useTableResizeState`) stay — they move with `PagedEditor`. Verified clean partition (no orchestrator hook imports a subtree hook). Mechanical `git mv` + 5 path transforms (both dirs at `components/<x>/hooks/` → same depth, only `DocxEditor`-internal + `editor`-internal paths shift) + repoint 2 `editor/` importers + the `fileOpen.test.tsx` tie. tsc clean first pass; green (1730 pass, knip clean). `DocxEditor/hooks/` now holds only the 8 subtree hooks.
  - **NEXT:** `DocxEditor/` now contains `PagedEditor.tsx`, `HiddenProseMirror.tsx`, `fileOpen.test.tsx`, + `hooks/` (8 subtree), `internals/`, `overlays/`. The remaining files are the **PagedEditor cluster** — `PagedEditor` + its 8 hooks + `overlays/` + the `internals/` it uses. This is the "different beast" (user): a careful deep-read unit with `/code-review` + smoke-test, NOT a mechanical move. `HiddenProseMirror` likely moves with/just before it. Optionally first: a quick look at `internals/` (leaf helpers — any dead/relocatable?) and whether `fileOpen.test.tsx` should move to `editor/`.

- **SHIPPED · Unit 12 — shim cull + UnifiedSidebar relocate (branch `refactor/editor-shims-and-sidebar`, user-guided "remove shims, it's clarity").** Three atomic commits: (1) relocated `components/UnifiedSidebar.tsx` → `components/sidebar/unified-sidebar.tsx` (joins its 10 sibling cards; `components/` is now all-subdirs, no loose files) — deep-read kept it (real collision-avoidance component, all props used); (2) **killed `commentFactories.ts`** — repointed its 4 core re-exports (`PENDING_COMMENT_ID`/`createCommentIdAllocator`/`CommentIdAllocator`/`createComment`) straight at core (`prosemirror/commentIdAllocator` + `prosemirror/commentOps`) across 7 import sites, moved the one real constant `EMPTY_ANCHOR_POSITIONS` to a new tracked `src/constants/comments.ts`; (3) **killed the `internals/tableResize.ts` + `internals/LayoutSelectionGate.ts` re-export shims** — repointed importers (1 + 5) to core. **PARKED `plugin-api/RenderedDomContext.ts`** (re-exported by the `plugin-api/index.ts` barrel = plugins' public contract → belongs to the separate PluginHost investigation). Green (1730 pass, knip clean = no orphans).
  - **NEXT (user-guided):** more "easy wins with hooks and stuff" — scan `DocxEditor/hooks/` + `internals/` for further shims / dead params / over-built helpers before the big files. THEN **`DocxEditor/PagedEditor.tsx`** (the paged ProseMirror host — composes `HiddenProseMirror` + the layout/pointer/scroll hooks + overlays; large + behavioural → its own careful unit with `/code-review` + smoke-test, NOT a mechanical move). `PagedEditor` and `HiddenProseMirror` are "different beasts" (user) — handle deliberately. Remaining in `DocxEditor/`: `PagedEditor`, `HiddenProseMirror`, `fileOpen.test.tsx` + the `hooks/`, `internals/`, `overlays/` subdirs.

- **SHIPPED · Unit 11 (PR #122, merged):** relocated `DocxEditor/DocxEditorPagedArea.tsx` → `editor/docx-editor-paged-area.tsx` (kebab; symbol `DocxEditorPagedArea` unchanged). Deep-read verdict: a legitimate presentational "body area" (sibling of the already-relocated `docx-editor-overlays`/`-dialogs`/`-toolbar`), all ~40 props genuinely used — no dead props, not a rewrite. Mechanical: `git mv` + repoint the 2 intra-`DocxEditor/` imports + the single importer + dropped a redundant single-child `<>` fragment at the call site. Green (1730 pass). NOTE `editor/`'s siblings were ALREADY relocated in the earlier UI-rework PRs — Unit 9's "NEXT = DocxEditorShell" was stale.

- **SHIPPED · Unit 9:** relocated `components/DocxEditor.tsx` → `components/editor/docx-editor.tsx` (kebab; pure mechanical move — it was already trimmed by units 1–8). Repointed its 42 relative imports (+1 `../` each) + the 3 external importers (`index.ts`, `ref-conformance.test-d.ts`, `useDocxEditorRefApi.ts`). Green (1726 pass).

## Discovery log

- **2026-06-30 · contract** — Mapped Patrick's contract (above) from the 5 `apps/` import sites. Confirmed the live `DocxEditor` prop set (9) + the UI `DocxEditorRef` method set (6).

- **SHIPPED · Unit 1 (PR #112, merged):** cut 9 unreachable `DocxEditorRef` methods (`getZoom/focus/loadDocument/loadDocumentBuffer` + 5 content-control) + impls + a stale contract-gate comment. Green, reviewed, −96 lines.

- **SHIPPED · Unit 8 (this branch) — BATCHED prop-surface collapse (method change for speed):** removed **17 never-passed props** in one PR (vs ~6 micro-PRs) — `mode`/`onModeChange` (controlled editing-mode → uncontrolled), `onOpen` (File>Open override → built-in only), `onError`, `onSelectionChange` (the host callback; the internal `selectionChangeSubscribersRef` fan-out + the ref-method `onSelectionChange` STAY), `showToolbar`/`showFileOpen`/`initialZoom`/`disableFindReplaceShortcuts`/`showOutline`/`showOutlineButton` (config → hardcoded to current defaults, behaviour-preserving), `fontFamilies`/`fonts`/`watermarkPresets` (→ built-in defaults; cascaded through the toolbar chain), `className`/`style`/`placeholder`. Deleted the orphaned `useFontLifecycle`. **Method:** compiler-guided (remove from interface+destructure, fix every `tsc` error — no grep-guessing past the heavy naming overlap of `className`/`style`/`onOpen`/`theme`/`mode`) + 4 parallel mechanical agents on disjoint hook/toolbar files. **Kept:** `theme` (deferred — rendering-fallback semantics) + the parked PluginHost/collab cluster (`externalPlugins`/`externalContent`/`onEditorViewReady`/`plugin*`/`document`). Green (1726 pass). **BROAD smoke-test:** toolbar visible, outline toggle, font dropdown, watermark menu, selection, file-open, editing-mode dropdown, error states. Net −224.

- **SHIPPED · Unit 7 (this branch):** removed 3 unused pure host-callbacks — `onSave` (Patrick saves via `ref.save()`'s return value, not this notify), `onFontsLoaded`, `onDocumentNameChange`. Pruned from `DocxEditor` props + `useFileIO` (params/calls/deps) + `useFontLifecycle` (param + the `onFontsLoaded` subscription effect + its core import); updated `fileOpen.test.tsx` (dropped the `onDocumentNameChange` harness wiring + assertion). Behaviour-identical for Patrick. NAMING TRAP avoided: the header/footer editors' own `onSave` (`InlineHeaderFooterEditor`) is unrelated and stays. Green (1728 pass). **Deferred (own cuts):** `onError` (wider — cascades through 4 hooks + the ErrorBoundary) and `onSelectionChange` (overloaded — also the internal selection-wiring name); plus the config-collapse toggles (`showToolbar`/`showFileOpen`/`showOutline`/`showOutlineButton`/`initialZoom`/`disableFindReplaceShortcuts`). PluginHost/collab still parked.

- **SHIPPED · Unit 6 (this branch):** removed the **4 granular comment-event host-callbacks** (`onCommentAdd`/`onCommentResolve`/`onCommentDelete`/`onCommentReply` — host notifications Patrick never passes). Surgical: removed the props + the optional `onComment*?.(…)` notify lines *inside* the internal `commentCallbacksRef` handlers (+ their now-orphaned `parent`/`target` locals) + the `onCommentDelete` plumbing through `useCommentManagement`'s orphan-cleanup. **Naming trap avoided:** the internal `CommentCallbacks` handlers (`onCommentReply`/`onCommentResolve`/`onCommentDelete` keys on `commentCallbacksRef` + the `stableCallbacks` delegation) are the load-bearing comment *operations* and STAY — only the host-notify side-calls went. Behaviour-identical for Patrick (callbacks were never provided). Green (1728 pass). Smoke-test add/resolve/delete/reply. (Remaining prop-surface: config-collapse toggles + misc pure callbacks like `onSave`/`onError`/`onSelectionChange`; PluginHost/collab still parked.)

- **SHIPPED · Unit 5 (this branch):** removed the **controlled-comments collaboration surface** (the Yjs/Liveblocks comment-sync mode local-first Patrick never uses). Cut the 4 props (`comments`/`onCommentsChange`/`commentsSidebarOpen`/`onCommentsSidebarOpenChange`), deleted `useControllableBoolean` + its test (replaced the one call site — `showCommentsSidebar` — with plain `useState`), and collapsed `useCommentManagement` to uncontrolled-only (dropped `commentsProp`/`onCommentsChange`/`isControlledComments` routing). **Behaviour-identical for Patrick** (the controlled branch was never executed — `commentsProp` never passed), but touches comment state → smoke-test add/resolve/delete + sidebar toggle. NOTE the naming trap avoided: `PagedEditor`'s internal `commentsSidebarOpen` prop (the *derived* open state from `DocxEditorPagedArea`) is unrelated to the removed external prop and stays. Green (1728 pass). The granular comment callbacks (`onCommentAdd/Resolve/Delete/Reply`) are left for the separate unused-callbacks bundle.

- **SHIPPED · Unit 4 (this branch, = 4a+4b folded):** removed the **interactive content-control (SDT) widget feature** end to end. **4a (visible half):** deleted `ContentControlWidgets.tsx` + its DocxEditor wiring; cut the core painter affordances (`sdtBoundary` checkbox/dropdown/date triggers + repeat buttons + `widgetKindFor`, the inline `renderParagraph` `applyInlineSdtWidgetAttrs`); removed the inline-widget data plumbing (`inlineSdtWidgets` module, the `TextRun.inlineSdtWidget` field, `toFlowBlocks` population + `inlineCheckboxWidgetFor`); deleted the 2 render-path tests + the dead widget CSS in both `editor.css` files. **4b (orphan sweep, folded in after review flagged it):** deleted the now-fully-orphaned `prosemirror/contentControls.ts` (PM-level find/set/remove/value/repeating-section TRs — all 9 exports 0-consumer post unit-1) + its test + the `prosemirror/index.ts` barrel re-export. **Kept:** SDT parse/serialize + the boundary-box display + hover/focus reveal **AND the entire `agent/` headless content-control layer** (live public API via `headless.ts` — NOT orphaned; the two same-named `contentControls.ts` files are distinct). Smoke-tested: SDT docs render controls as plain text + round-trip. Green (1733 pass).

- **SHIPPED · Unit 3 (this branch):** removed the **entire i18n system** (English-only). Inlined all 73 `t('key')` call sites across 13 files to English literals (small parallel mechanical agents + central compiler verification), inlined the `editing-modes` labels + the `useContextMenus` table labels + the `image-context-menu` dynamic keys (local lookup), removed the `LocaleProvider` wrapper + the `i18n` DocxEditor prop, deleted `src/i18n/` + the whole `@eigenpal/docx-editor-i18n` package (now 3 vendored editor packages, not 4), and updated CLAUDE.md + knip/tsconfig/package.json/lockfile. Green (1751 pass). Net −1889 lines.

- **SHIPPED · Unit 2 (this branch):** cut the orphaned **`applyTableStyle` dead chain** — `table-group.tsx` (the only `TableAction` emitter) never emits `applyTableStyle` (the `TableStyleGallery` that did was removed in Phase 1). Deleted `internals/table-style-presets.ts`, the `applyTableStyle` member in `types/table.ts`, the handler branch + 3 imports in `useTableDialogs`, and the now-orphaned `historyStateRef`/`getCachedStyleResolver` params it dragged (pruned from the hook + its `DocxEditor.tsx` call site). `borderSpecRef` stays (used by the border actions). Green.

- **QUEUED · `ContentControlWidgets` cut (decided, deferred for momentum).** Sizing discovered 2026-06-30: it's NOT a one-file delete — it's a ~10-file **cross-package** interactive-SDT feature: React `ContentControlWidgets.tsx` + DocxEditor wiring · core block painter (`sdtBoundary.ts` L82-125 triggers + `widgetKindFor`) · core inline painter + data plumbing (`renderParagraph/runs.ts` + `layout-bridge/toFlowBlocks/runs.ts` + the `inlineSdtWidget` field on `TextRun` in `layout-engine/types.ts`) · 4 tests (`inline-sdt-widget`, `block-sdt-boundary`, `inline-sdt-checkbox`, agent `contentControlValues`) + the orphaned core mutation helpers (`setContentControlValueTr` family). Clean/separable (keep the boundary box + hover/focus reveal + parse/serialize), but touches core's tested layout pipeline → **needs a smoke test**. Do as a dedicated medium unit after the lean cuts.

- **2026-06-30 · `DocxEditor.tsx` deep-read (1802 lines) — DONE.** Verdict: **keep / right-home / NOT a rewrite** — it's the entry + orchestrator; logic lives in ~25 hooks, this file is wiring + comment/sidebar glue. **fit = over-built:** it exposes a generic editor-*library* API (collaboration/Yjs, plugin-host, MCP, i18n, controlled-everything) to a single consumer. The dead weight is whole API *categories*, not scattered orphans.
  - **Ref-API reachability (closed both ends).** Of ~40 ref methods, **22 reachable, KEEP:** UI(6)=`save/getAgent/getCurrentPage/getTotalPages/setZoom/openFind`; agent-bridge(16, from `grep editorRef.* in packages/docx-editor-agents`)=`addComment/applyFormatting/findInDocument/getComments/getDocument/getEditorRef/getPageContent/getSelectionInfo/insertBreak/onContentChange/onSelectionChange/proposeChange/replyToComment/resolveComment/scrollToParaId/setParagraphStyle`.
  - **DECISION — first cut = 9 DEAD ref methods:** `getZoom`, `focus`, `loadDocument`, `loadDocumentBuffer` + the 5 content-control methods (`getContentControls/scrollToContentControl/setContentControlContent/removeContentControl/setContentControlValue`). 0 UI + 0 agent use. Content-control methods are thin delegations to core `*Tr`/`find*` fns (`useDocxEditorRefApi.ts:197-256`) — **cutting them does NOT affect a docx that contains SDTs** (parse = `core/docx/sdtProperties.ts`; render = `ContentControlWidgets.tsx`; both independent). Cut their impls in `useDocxEditorRefApi`; **at cut-time check** whether the core `*Tr`/`find*` fns get orphaned (nothing else uses them → cut too; else leave core). Re-add later against real call sites if needed (cheap).
  - **DECISION — KEEP the 6 scroll/highlight methods** (`scrollToPage/scrollToPosition/scrollToParaId/scrollToCommentId/scrollToChangeId/highlightRange`) — NOT dead, they're **near-term roadmap infra** (user, 2026-06-30): undo/redo→`scrollToPosition`; chat "click a change → scroll to it"→`scrollToChangeId`/`scrollToParaId`; claim-chart "highlight the basis passage"→`highlightRange`+`scrollToPosition`. **But "keep" ≠ "leave alone": they need a verify+fix pass** — likely partly broken (carry-forward already flags `scrollToParaId` double-flash + HF-fallback scroll gaps in `scrollToChangeId`/`scrollToParaId`). This is its own review unit (the `usePagedScrollApi`/`usePagedEditorRefApi` cluster), and the foundation to land before wiring those features.
  - **NEW BUG (user, 2026-06-30):** typing past the page edge / pressing Enter-Enter does **not** scroll the caret into view — cursor runs off the viewport. Same scroll machinery (caret-follow-on-edit). Fold into the scroll verify+fix unit (or SCRATCHPAD).
  - **Prop-surface finding (LATER strangler, not now):** Patrick passes 9 of ~50 props. Removable categories cascade into hooks: collab/Yjs (`externalContent`/`externalPlugins`/`onEditorViewReady`), controlled-comments-for-collab (`comments`/`onCommentsChange`/`commentsSidebarOpen`/`onCommentsSidebarOpenChange` → `useCommentManagement`), `i18n`, + unused callbacks/toggles. **DECISION (user, 2026-06-30): the PluginHost surface is EXCLUDED from this cut** (`pluginOverlays`/`pluginSidebarItems`/`pluginRenderedDomContext`/`onRenderedDomContextReady`/`externalPlugins` + `plugin-api/`) — the user likes plugins as the feature-extension mechanism but is unsure of its effectiveness/usefulness → **needs a separate investigation unit** (evaluate the plugin-api/ + PluginHost mechanism before keep-or-cut), NOT a blind cut. The rest (collab/Yjs minus plugins, controlled-comments, i18n) still goes. Multi-step; after the self-contained ref cut.
  - **Brain bugs (checklist):** `(entry as any).hfRid = rId` untyped mutation (L658); `onAcceptChangeById`/`onRejectChangeById` are two near-identical ~25-line HF-view-scan blocks → dedupe (L1272-1325); always-truthy guard `if (!match && revisionIdAliases)` (L1463).
  - **DECISION (user, 2026-06-30): CUT `ContentControlWidgets`** (rendered SDT UI — "don't care for it"). Its own unit: removing it changes render behaviour for SDT-containing docs (the SDTs become plain content) → **needs a smoke test**. When cut, it's the last react consumer of the core content-control `*Tr`/`find*` fns → re-check core orphans then (those core fns + the parser support may also become cuttable).

## Carry-forward (from `editor-audit.md` — reference, don't duplicate)

Phase 1+2 shipped (~2,300 lines gone; PRs #90–#95): dead `ui/` pickers, dead hooks, the legacy table path, the print-preview feature, `renderAsync`, trimmed chrome props, orphaned core managers. The **bug checklist** (`HiddenProseMirror.getDocumentId` collision, `useLayoutTriggers` stale closures, `ImageSelectionOverlay` zoom/right-click/scroll bugs, `useTableResizeState` handle drift, `useDocumentLoader` generation race, `usePagesPointer` dead branch, `useTableDialogs` `tableProperties` no-op, `useVisualLineNavigation` core dupe, `useScrollPageInfo` hardcoded gaps, `SelectionOverlay` double-timer, `useImageActions` phantom dialog) + the **keep-notes** live in `editor-audit.md §Phase 3/Keep-notes` — pull the relevant one into each file's pass.

**Deferred (own passes):** headers/footers (render-only → editable) and plugins (`plugin-api/`, core-plugins) — sub-bugs in `SCRATCHPAD.md`.
</content>
