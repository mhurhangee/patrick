// Practice-context default (the shared <PRACTICECONTEXT> text). Empty by
// default — the attorney fills it. The per-surface system prompts now live as
// full templates in ./prompt/templates.ts (DEFAULT_TEMPLATE_*).
export const DEFAULT_PROMPT_CONTEXT = ""
