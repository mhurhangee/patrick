# Bun is Patrick's test runner. Discovery is scoped to packages/ (the vendored
# docx-editor suites today; Patrick's own tests will live here too). Run `bun
# test` from the repo root so the editor's round-trip tests resolve their
# fixtures via process.cwd() at e2e/fixtures/.
[test]
root = "./packages"
